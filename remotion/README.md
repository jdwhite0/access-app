# ACCESS marketing Remotion

Two compositions:

1. **AccessHeroAmbient** — 20s calm void loop for homepage hero canvas.
2. **AccessProductScene** — 14s product UI with `scene` prop: `home` | `guide` | `plans`.

Render outputs land in `../public/marketing/video/`.

```bash
npm install
npm run studio
npm run render:all
```
