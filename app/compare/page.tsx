import ComparePageClient from '@/components/platform/ComparePageClient'

export const metadata = {
  title: 'Compare Plans — ACCESS',
  description: 'Full feature comparison across Personal, Builder, and Enterprise — plus how ACCESS compares to Notion, ClickUp, HubSpot, and ChatGPT.',
}

export default function ComparePage() {
  return <ComparePageClient />
}
